export default function CompareView() {
  return (
    <div className="flex items-center justify-center h-full text-[rgb(var(--color-text-muted))] text-sm">
      Compare view — coming in Phase 4
    </div>
  )
}
