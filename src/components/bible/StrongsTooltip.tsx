interface StrongsTooltipProps {
  children: React.ReactNode
  strongsNum: string
}

// Phase 1 stub — Phase 4 will add @radix-ui/react-tooltip with real lexicon data
export default function StrongsTooltip({ children }: StrongsTooltipProps) {
  return <>{children}</>
}
